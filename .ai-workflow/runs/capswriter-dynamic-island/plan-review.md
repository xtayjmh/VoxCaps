# 计划评审

APPROVED

## 结论

计划覆盖快捷键、录音、识别结果、文字输出、UI 生命周期和双包构建，没有修改服务端协议或模型推理，范围与用户需求一致。

## 必须遵守

- UI 线程异常不得传播到识别主流程。
- 送达状态必须发生在文字输出成功之后。
- 短录音取消不得进入识别状态。
- 迟到结果必须按任务 ID 隔离。
- 完整包与纯客户端包都要验证包含新增模块。

## 剩余风险

- 独立 reviewer 在当前环境不可用，本次降级为主会话隔离自审。
- Windows 无边框透明窗口在不同 DPI/多屏环境需要实机验证。
